# Core dependencies
# Pinned: a2a-sdk 1.x is protobuf-backed and drops the camelCase aliases this
# code relies on (AgentCapabilities.pushNotifications, AgentCard.url), so an
# unpinned build breaks at import. These are the versions running in v0.0.22.
google-adk[a2a]==1.28.1
a2a-sdk==0.3.25
litellm>=1.80.0
python-dotenv>=1.0.0

# Production server
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
gunicorn>=23.0.0

# Database session storage (PostgreSQL)
asyncpg>=0.29.0
sqlalchemy>=2.0.0
